🚀 autopip
نصب‌کننده‌ی خودکار کتابخونه‌های پایتون – ساخته شده توسط FrameworkPython

------------------------------------------------------------
✨ معرفی
وقتی یه اسکریپت پایتون رو اجرا می‌کنی، خیلی وقتا با خطای ModuleNotFoundError مواجه می‌شی چون یه کتابخونه نصب نیست.
autopip این مشکل رو حل می‌کنه:
- سورس کد رو می‌خونه
- ایمپورت‌ها رو تشخیص می‌ده
- اسم ماژول رو به پکیج درست روی PyPI نگاشت می‌کنه
- و خودش به صورت خودکار نصب می‌کنه ✅

دیگه لازم نیست دستی دونه‌دونه pip install بزنی.

------------------------------------------------------------
⚡ ویژگی‌ها
- شناسایی خودکار کتابخونه‌ها از روی کد
- نگاشت هوشمند ماژول → پکیج (مثلاً cv2 → opencv-python)
- نصب بی‌صدا (silent) بدون اسپم اضافی
- خروجی رنگی و مرحله‌ای برای تجربه‌ی کاربری بهتر
- لاگ‌گیری ساده برای بررسی بعدی
- بنر ASCII خوشگل برای شروع 😎

------------------------------------------------------------
🛠 نصب
pip install autopip

------------------------------------------------------------
🎯 استفاده
اجرای مستقیم روی یه فایل:
autopip myscript.py

مثال:
فرض کن اسکریپتت این ایمپورت‌ها رو داره:
import requests
import numpy as np

وقتی اجرا کنی:
autopip myscript.py

خروجی چیزی شبیه این میشه:
The following libraries were identified:
- requests
- numpy

Finding libraries that are not installed . . .

These libraries are not installed:
- requests
- numpy

Installing library requests ...
✔ requests installed

Installing library numpy ...
✔ numpy installed

All libraries have been installed

------------------------------------------------------------
📋 امکانات CLI
- اجرای روی فایل خاص:
  autopip myscript.py
- حالت silent (بدون خروجی اضافی):
  autopip myscript.py --silent
- لاگ‌ها داخل فایل autopip.log ذخیره میشن.

------------------------------------------------------------
🔗 لینک‌ها
GitHub: https://github.com/FrameworkPython/autopip
PyPI: https://pypi.org/project/autopip/

------------------------------------------------------------
📜 مجوز
این پروژه تحت مجوز MIT منتشر شده. یعنی می‌تونی آزادانه استفاده، تغییر و منتشر کنی، فقط کپی‌رایت رو حفظ کن.

------------------------------------------------------------
❤️ سازنده
FrameworkPython – با عشق برای جامعه‌ی پایتون
